# acnvrk.github.io

The published copy of **[Notes by Kishore](https://acnvrk.pages.dev)**.

This repository holds generated output only. The site is built from a private
source repository and force-pushed here to the `gh-pages` branch on every
build, so **any commit made to `gh-pages` directly will be overwritten without
warning.**

The canonical site is <https://acnvrk.pages.dev>. This mirror exists so the
`acnvrk.github.io` address keeps working.

## Contributions

Not accepted -- see [CONTRIBUTING.md](CONTRIBUTING.md). Pull requests opened
here are closed automatically.
